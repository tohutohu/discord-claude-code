#!/usr/bin/env -S deno run -A

/**
 * Discord Claude Code CLI エントリーポイント
 *
 * このファイルはプレースホルダーとして作成されています。
 * 実際の実装はPR-2以降で行われます。
 */

console.log('Discord Claude Code CLI - Coming soon!');
console.log('This is a placeholder for the main CLI application.');
console.log('');
console.log('Current implementation status:');
console.log('✅ Project setup and CI/CD pipeline');
console.log('🚧 CLI and TUI implementation (PR-2)');
console.log('🚧 Discord integration (PR-3)');
console.log('🚧 Repository management (PR-3.8)');

// バージョン情報を表示（versionサブコマンドのプレースホルダー）
if (Deno.args[0] === 'version') {
  console.log('\nVersion: 0.1.0-dev');
  Deno.exit(0);
}
